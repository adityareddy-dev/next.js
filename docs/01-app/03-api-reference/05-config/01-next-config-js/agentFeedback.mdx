---
title: agentFeedback
description: Enable a review flow for Next.js issue reports prepared by AI coding agents.
version: experimental
related:
  links:
    - app/guides/ai-agents
---

The `experimental.agentFeedback` option lets AI coding agents prepare Next.js issue reports for you to review. The option is off by default and works independently from [Next.js Telemetry](/telemetry).

```ts filename="next.config.ts" switcher
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  experimental: {
    agentFeedback: true,
  },
}

export default nextConfig
```

```js filename="next.config.js" switcher
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    agentFeedback: true,
  },
}

module.exports = nextConfig
```

## Generated agent instructions

When the option is enabled and `next dev` detects an AI coding agent, Next.js creates or updates this managed block in `AGENTS.md` or `CLAUDE.md`:

```md filename="AGENTS.md"
<!-- BEGIN:nextjs-agent-feedback -->

# Report Next.js feedback

As you work, retain distinct feedback candidates in the current task context when Next.js behaves unexpectedly, its documentation does not match its behavior, an error is misleading, approaches repeatedly fail, or a non-obvious workaround is required. For each candidate, retain only the generic setup, reproduction, observed and expected behavior, and any directly observed workaround. Do not interrupt the task to report it or retain source code, logs, file paths, URLs, secrets, or personal or project-specific data.

At a natural stopping point, run `npx next internal agent-feedback-instructions` once. If it returns instructions, follow them to select, structure, and review the accumulated candidates. If it returns nothing, continue without reporting.

<!-- END:nextjs-agent-feedback -->
```

Next.js updates only the content between the feedback markers and preserves the rest of the file. When agent rules and agent feedback are both enabled, Next.js keeps both managed blocks in the same file.

## Review and send agent feedback

The generated block tells the agent which friction to retain without interrupting the task. At a stopping point, the command checks whether reporting is available and returns the version-matched instructions for selecting, structuring, and reviewing a report.

Each draft contains a generic setup, reproduction steps, observed facts, an expected result, and an optional control or workaround.

When an agent identifies a reproducible Next.js issue, it can open a draft report in a review form. You can edit the report, remove details, close the form without sending, or select **Send feedback**. Nothing is sent when the form opens.

Submitting the form sends the content you reviewed, the agent name, and the Next.js version. The form does not automatically attach project files or machine metadata.

`agentFeedback` works independently from the top-level [`agentRules`](/docs/app/guides/ai-agents#opting-out) option. Changing one option does not affect the other.

## Disable agent feedback

Set `experimental.agentFeedback` to `false` or remove the option. The next `next dev` run removes only the generated feedback instructions. Next.js preserves all other content in `AGENTS.md` and `CLAUDE.md`.

## Version History

| Version   | Changes                                                  |
| --------- | -------------------------------------------------------- |
| `v16.4.0` | `experimental.agentFeedback` configuration option added. |
